import React from 'react';

// All authenticated users can access the app freely.
// The free trial prompt is shown contextually in ListDetail after first price comparison.
export default function SubscriptionGate({ children }) {
  return children;
}